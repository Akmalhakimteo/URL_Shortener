import Button from "./Button";

const History = () => {
  return (
    <history className = "buttonrow">
      <h2>history</h2>
      <Button className = text="show" />
    </history>
  );
};

export default History;
